# Copy this file to `.env` and customize before running the project
# Local Postgres example (used by `server/db.ts` via `DATABASE_URL`)
DATABASE_URL=postgres://emily:pass@localhost:5432/emily_db

# Frontend -> API proxy target (optional)
VITE_API_TARGET=http://localhost:3000

# API server port (optional)
PORT=3000

# Other optional placeholders
RESEND_API_KEY=
SUPABASE_URL=
SUPABASE_ANON_KEY=

# NOTE: If you use the included docker-compose, the example DATABASE_URL above will work
